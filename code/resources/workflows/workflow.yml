jobs:
  - name: my_workflow
    tasks:
      - task_key: task_1
        notebook_task:
          notebook_path: ${notebook_path}  # Parameterized notebook path
        new_cluster:
          spark_version: 13.3.x-scala2.12  # Use the latest serverless runtime version
          node_type_id: "serverless"       # Serverless node type
          num_workers: 1                   # Number of workers (adjust as needed)
